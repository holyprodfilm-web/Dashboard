-- =============================================================
-- Назначение первого администратора
-- =============================================================
-- Запустите этот скрипт в SQL-редакторе Supabase Dashboard:
--   https://app.supabase.com → ваш проект → SQL Editor → New query
--
-- Замените <email> на email нужного пользователя и выполните запрос.
-- =============================================================

UPDATE profiles
SET role = 'admin'
WHERE email = '<email>';

-- Проверка — убедитесь, что запись обновилась:
SELECT id, email, full_name, role
FROM profiles
WHERE email = '<email>';
